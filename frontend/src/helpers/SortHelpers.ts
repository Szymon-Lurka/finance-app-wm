import type {DefaultSortDetails} from "@/types/models/Sorting";

const defaultSortDetails: DefaultSortDetails = {
    order: 'ascending',
    parameter: 'createdAt'
}

export {
    defaultSortDetails
}